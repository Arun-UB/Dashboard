// Record.js
// --------
define(["jquery", "backbone"],

    function($, Backbone) {

        // Creates a new Backbone Record class object
        var Record = Backbone.Model.extend({

        });

        // Returns the Record class
        return Record;

    }

);
